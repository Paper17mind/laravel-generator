use Illuminate\Support\Facades\Route;
use App\Http\Controllers\Api\{{ resourceUpper }}Controller;

Route::prefix('api')->group(function () {
    Route::apiResource('{{ resourcePlural }}', {{ resourceUpper }}Controller::class);
});
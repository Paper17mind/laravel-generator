package middleware

import (
	"github.com/gin-gonic/gin"
	"log"
)

func {{ resourceUpper }}Middleware() gin.HandlerFunc {
	return func(c *gin.Context) {
		log.Printf("{{ resourceUpper }} Middleware: %s %s", c.Request.Method, c.Request.URL.Path)
		c.Next()
	}
}

<?php

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration
{
    public function up(): void
    {
        Schema::create('{{ resourcePlural }}', function (Blueprint $table) {
            $table->id();
        {%- for column in columns %}
            {%- if column.name != 'id' %}
            {%- if column.type == 'string' %}
            $table->string('{{ column.name }}'){% if column.nullable %} ->nullable(){% endif %};
            {%- elseif column.type == 'text' %}
            $table->text('{{ column.name }}'){% if column.nullable %} ->nullable(){% endif %};
            {%- elseif column.type == 'integer' %}
            $table->integer('{{ column.name }}'){% if column.nullable %} ->nullable(){% endif %};
            {%- endif %}
            {%- endif %}
        {%- endfor %}
            $table->timestamps();
        });
    }

    public function down(): void
    {
        Schema::dropIfExists('{{ resourcePlural }}');
    }
};
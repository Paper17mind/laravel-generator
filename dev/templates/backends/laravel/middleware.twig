<?php

namespace App\Http\Middleware;

use Closure;
use Illuminate\Http\Request;

class {{ resourceUpper }}Middleware
{
    public function handle(Request $request, Closure $next)
    {
        // Logic for {{ resourceUpper }} middleware
        return $next($request);
    }
}

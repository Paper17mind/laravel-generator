# 🚀 {{ project_name }} - Generated by CodeGen v2

This project was generated with a full Docker setup. You can run the entire stack with one command.

## 🛠️ Prerequisites
- [Docker](https://docs.docker.com/get-docker/)
- [Docker Compose](https://docs.docker.com/compose/install/)

## ⚡ Quick Start

1. **Run the stack:**
   ```bash
   docker-compose up -d --build
   ```

2. **Access your applications:**
   - **Frontend:** [http://localhost:3000](http://localhost:3000)
   - **Backend API:** [http://localhost:8000/api](http://localhost:8000/api)
   - **Database:** `localhost:3306` (User: `root`, Pass: `root`)

3. **Initialize Database (Laravel):**
   ```bash
   docker-compose exec api php artisan migrate --seed
   ```

## 🏗️ Tech Stack
- **Backend:** {{ backend_framework|capitalize }}
- **Frontend:** {{ frontend_framework|capitalize }}
- **Database:** MySQL 8.0
- **Containerization:** Docker

---
Generated by **CodeGen v2** at {{ "now"|date("Y-m-d H:i:s") }}

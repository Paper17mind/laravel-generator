<template>
  <q-layout view="lHh Lpr lFf">
    <q-header elevated class="bg-indigo-7 text-white">
      <q-toolbar>
        <q-btn flat dense round icon="menu" @click="leftDrawerOpen = !leftDrawerOpen" />
        <q-toolbar-title class="text-weight-bold">{{ project_name|capitalize }} Admin</q-toolbar-title>
      </q-toolbar>
    </q-header>

    <q-drawer v-model="leftDrawerOpen" show-if-above bordered class="bg-white">
      <q-list padding>
        <q-item-label header class="text-overline text-grey-7">Modules</q-item-label>
        
        {% for table in tables %}
        <q-item 
          clickable 
          v-ripple 
          :to="'/{{ table.name }}'"
          active-class="bg-indigo-1 text-indigo-7"
        >
          <q-item-section avatar>
            <q-icon name="table_chart" />
          </q-item-section>
          <q-item-section>
            <q-item-label>{{ table.name|capitalize }}</q-item-label>
          </q-item-section>
        </q-item>
        {% endfor %}
      </q-list>
    </q-drawer>

    <q-page-container>
      <!-- Router View renders the active component based on URL -->
      <router-view v-slot="{ Component }">
        <transition enter-active-class="animated fadeIn" leave-active-class="animated fadeOut" mode="out-in">
          <component :is="Component" />
        </transition>
      </router-view>
    </q-page-container>
  </q-layout>
</template>

<script setup>
import { ref } from 'vue'
const leftDrawerOpen = ref(false)
</script>
